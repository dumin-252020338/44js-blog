html
    head
        title 44-js   
        style
            include nav.css
        style        
            include reg.css
        
        //- script(src="https://cdn.bootcss.com/jquery/3.3.1/jquery.min.js")
        

    body
        include nav-con.pug
        div(id="wrap")
            div(class="img")
                //- img(src="img/demo2-1/3.jpg" alt="")
            input(class="phone", type="text", placeholder="手机号")
            input(class="name", type="text", placeholder="用户名")
            input(class="pwd", type="text", placeholder="密码")
            input(class="pwds", type="text", placeholder="再次输入密码")
            input(class="email", type="text", placeholder="邮箱")
            input(class="code", type="text", placeholder="验证码")
            input(class="con")
            div(class="phPoint") 请输入11位手机号
            div(class="NaPoint") 请输入4到16位（字母，数字，下划线，减号）
            div(class="pwdPoint") 最少6位，至少包含大、小写字母，数字，特殊字符各1个
            span 可以使用的手机号
            span 不合法手机号
            span 不合法的用户名
            span 可以使用的用户名
            span 密码强度弱
            span 密码强度高
            span 两次输入的密码不一致
            span 两次输入的密码一致
            span 邮箱不存在或已注册
            span 可以使用的邮箱
            span 验证码错误
            span 验证码正确
            span 
            button 注册
    script
            include reg.js
